# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

Milestones 0–8 built; M9 (the Three.js renderer) and M10 (multi-table + Tidebreaker, below) are in progress on this branch (`feature/renderer-3d`): `src/render/Renderer3D.ts` implements the same `Renderer` seam — planar physics maps `(x, y)` → scene `(x, z)` per plan §7, the playfield SVG raster textures the table plane and its collision polylines are extruded into rails so 3D walls match physics by construction, and the DMD/backglass mount as a DOM side panel; a "3D camera" settings row switches tilted perspective ↔ top-down orthographic (a WebGL-drawn classic view, persisted in `pinball-3d-view-v1`) via the optional `Renderer.setView3D` seam method. Players toggle 2D ↔ 3D in the settings overlay (persisted in `pinball-render-mode-v1`, deliberately outside `Tuning` so tuning resets can't flip it); `Game.applyRenderMode` swaps renderer + canvas at runtime (a 2D and WebGL context can't share one canvas) and three.js loads lazily as its own chunk, so 2D players never download it. Stack: Vite + TypeScript + Planck.js; Canvas-2D renderer behind the `Renderer` interface; scrolling camera; full element set (flippers, bumpers, slings, drop-target bank, rollovers, orbit + spinner, telescope kickout scoop — `src/entities/Kicker.ts` captures the ball for the DMD "observation" then ejects at `tuning.kickerEject`; the scoop geometry never traps, only the sensor+hold logic keeps a ball) scoring via the EventBus; and the M4/M5 layer — game flow (attract → 3-ball play → game over), ball-saver, nudge/tilt, moon-lane bonus multipliers, localStorage high scores, the DMD subsystem (`src/render/dmd/`: `DotMatrix` 128×32 amber surface + 5×7 bitmap font, `DmdScene`, `DmdQueue`; Game owns it, renderers only composite its canvas), baked DMD event animations (SVG sprite-strip masters in `design/dmd-scenes/` quantized to dot frames at load by `dmd/bake.ts` — never live SVG on the grid), in-world juice (camera shake, pooled impact rings, ball trail), and audio (`src/audio/`: `AudioEngine` — Web Audio buses + synthesized chip SFX per event, unlocked on first gesture; `ChipMusic` — procedural chiptune sequencer playing an original theme; tracker-module `.xm` playback can later replace `ChipMusic` on the same music bus; volumes in the tuning panel). M7: scoring values and mode parameters live in `design/tables/moondial/rules.json` (never hardcode points — `Scoring.ts` reads base awards; `src/game/moondial.ts` runs orbit combos, end-of-ball bonus, the LUNAR ECLIPSE mode and the telescope sightings ladder — escalating scoop awards that wrap, the last spotting an orbit toward the eclipse — from it); backglass art at `design/tables/moondial/backglass.svg` renders in the side panel under the DMD. M8: attract-mode lamp show, DMD high-score initials entry (flippers cycle, plunger confirms), and a player-facing settings overlay (Esc — volumes + full key remapping via `src/ui/SettingsPanel.ts`; `Input` bindings persist in localStorage; game pauses while open). The 2D renderer is **plain Canvas 2D by explicit decision — keep it that way** (no PixiJS/WebGL 2D); it stays the default mode, with `Renderer3D` as the opt-in alternative behind the same interface. When changing any `DEFAULT_TUNING` value, update `docs/tuning/current-defaults.md` (change log + rationale); `docs/tuning/original-defaults.md` is a frozen baseline. The build plan at `docs/plans/pinball-build-plan.md` remains the source of truth — **read it before any implementation work**. Flipper feel is player-approved; physics tuning stays exposed in the debug panel.

**M10 (multi-table):** the engine is table-agnostic behind two seams — `src/table/specs.ts` (`TableSpec`: per-table geometry/scoring-rules/logic, Node-safe, consumed by Game AND the sim scripts) and `src/table/assets.ts` (Vite-only raw SVG/DMD-scene/song imports). Per-table entity constants live in `src/table/defs/<id>.ts` (`src/table/geometry.ts` keeps only shared types + flipper/ball constants); per-table rules logic (modes, multiplier lanes, kicker award ladders, DMD narration) implements `src/game/TableLogic.ts` — `src/game/moondial.ts`, `src/game/tidebreaker.ts` (the old `game/Modes.ts` is gone). Players switch tables in the settings overlay (persisted in `pinball-table-v1`, applied via reload in `main.ts` — the renderer swap stays live, the table swap deliberately doesn't); high scores are per-table. Collision **layers** (physics stays planar, plan §7): `data-layer` on SVG walls/sensors → Planck filter bits; `sensor-layer-*` sensors switch the ball's layer (`data-to-layer`; `data-up-only` guards open-field ramp entries against sideways strays; all switch guards live in `Ball.queueLayerSwitch`, which re-validates the ball is still inside the zone when the queued switch applies — a corner-grazing ricochet must not be switched outside the target layer's walls); `height-profile-*` paths give renderers display height (2D scales/dims the ball + shadow, 3D really elevates the ball and builds elevated rail tubes). New entities: `Subway` (scripted under-field transit along a height profile) and multiple `Kicker`s per table incl. lit-gated kickbacks (`TableLogic.kickerLit`/`onCapture`); `Game` runs an off-rail/stall safety net that grounds a stranded layer-1 ball, plus an out-of-bounds net that drains a ball escaping the table envelope (a mis-layered ball ignores layer-0 walls incl. the shell — it can never reach the drain sensor outside); soak flags any OOB ball as a failure. Table 2 is **Tidebreaker** (abyssal salvage dive — design truth in `design/tables/tidebreaker/BRIEF.md`, incl. v1 deltas from the concept): D-I-V-E depth gauge → bonus multiplier + escape-hatch kickback, winch ramp + habitrail circuit (layer 1, spinner as the reel), dive-bell haul ladder, trench + gutter subways, airlock bank recessed in the orbit wall (right-flipper cross shot), LEVIATHAN wizard mode; abyss field-token variant in `design/tokens.css`; per-table chiptune themes in `src/audio/songs.ts` (`ChipMusic` performs a passed `Song`). The drain sensor is ignored while a kicker/subway holds the ball captive — saving subways (Tidebreaker's gutter, Midway's chicken exit) legitimately cross the drain zone; Game, simcheck and soak all carry the same guard.

**Table 3 is Midnight Midway** (funfair after dark — design truth in `design/tables/midway/BRIEF.md`, incl. v1 deltas; the lineup's nearest IP adjacency: the brief's §0 lists what must NOT resemble Pinball Fantasies' Partyland). It introduced the engine's **third flipper**: `TableGeometry.flippers.upper` (optional `{x, y, side}`, placed by `anchor-flipper-upper` and validated like the pair), the `upper` Input action (defaults to sharing the right-flipper keys; remappable in settings), and Game/simcheck/soak drive `flippers[2]` when present. THE MALLET (upper-right, main layer, base sealed 12 mm off the lane wall; the lane wall's top curls into a shield so arch drops shed onto the bat instead of perching) owns the HIGH STRIKER: a layer-1 wire lane over the dodgem arena to a bell above the P lane, graded by `sensor-lane-striker-a/b` timing gates in `src/game/midway.ts` (DING/STRONG/FAIR; roll-back = WEAK) with an up-only entry throat of small zones centred on the lane's centreline (mid-bat swings launch past the root curl; a zone edge within a ball radius of a channel wall switches an embedded ball and the solver ejects it as a layer-1 ghost — soak-found, hence small centred zones). Other rides: COASTER (left-flipper layer-1 circuit, two-crest chained height profiles, opposite-handed from the winch), GHOST TRAIN subway behind a turnstile spinner, DROP TOWER bank in the lane wall (left-flipper cross shot) lighting the PRIZE BOOTH scoop ladder, the ferris-wheel gondola ring (5 lamps; P-A-R-K completions, striker DINGs and the PANDA load gondolas — a full wheel steps the bonus-X), HAND STAMP kickback, CHICKEN EXIT (right-outlane subway back to the plunger lane when lit), and the RIDE PASS (punch all five rides) → FIREWORKS FINALE wizard mode. Carnival field variant + marquee-bulb token in `design/tokens.css`.

The global style guide (plan §5a, milestone 0.5) lives at `design/STYLE-GUIDE.md` with tokens in `design/tokens.css` and rendered specimens in `design/previews/` — these are the editable masters, synced to the Claude Design project "Pinball Reverie — game design system". **All art must inherit from it** (palette tokens, 1 SVG unit = 1 mm, collision/sensor/anchor layer naming, ball-gap rules); amend the guide before introducing any new color or convention.

Commands: `npm run dev` (Vite dev server), `npm run build` (typecheck + bundle), `npm run simcheck [table]` (headless physics smoke test, both tables by default — run after any physics or playfield-SVG change), `npm run soak [seed] [table]` (randomized 10-min play sim that flags ball traps; runs every table by default). Milestone 3.5 is done: table 1 is **Moondial** (night-observatory theme) and its shape truth is `design/tables/moondial/playfield.svg` — collision walls, sensors, anchors and height profiles are parsed from named layers by `src/table/SvgCollision.ts` (the visible wall strokes ARE the collision paths); each table's entity placements in `src/table/defs/<id>.ts` are cross-checked against its SVG's `anchor-*` markers at load. Run simcheck + soak after ANY edit to a playfield SVG.

## What this is

A browser-based retro pinball game in the spirit of Pinball Fantasies / Pinball Dreams: a vertically-scrolling tall playfield (several screens high), realistic physics, a dot-matrix display (DMD) for score and event animations, and chiptune audio. All visuals are authored in Claude Design as editable SVG masters.

## Planned stack

Vanilla TypeScript + Vite (no framework). Physics: **Planck.js** (Box2D port — chosen over Matter.js for continuous collision detection with fast small bodies). Renderer v1: 2D Canvas/PixiJS; v2: Three.js. Audio: Web Audio API + tracker-module playback (`.xm`/`.mod`) for music. Once scaffolded, expect standard Vite commands (`npm run dev`, `npm run build`).

## Architecture invariants

These are the decisions the whole plan hinges on — don't compromise them for convenience:

1. **Physics ↔ renderer decoupling.** Game state lives in the Planck world; renderers only *read* it each frame through a `Renderer` interface (`init`, `drawFrame`, `spawnEffect`). This is what makes the later 2D→3D swap possible. `Game` must never know which renderer it's talking to.
2. **Simulate in meters, not pixels.** Box2D wants 0.1–10m bodies (ball ≈ 0.027m). Scale to pixels only at render time.
3. **Fixed timestep + bullet ball.** Step physics at a fixed 1/120s; mark the ball as a bullet body for CCD. Variable timesteps and tunnelling are the two classic pinball failure modes.
4. **SVG is the single source of truth for table shape.** Collision geometry is derived from named layers/paths in the Claude Design playfield SVG (`collision-wall-*`, `sensor-ramp-entry-*`); the table's JSON carries only rules metadata (scoring, modes, lights). Never hand-author collision bodies that duplicate drawn geometry. A debug overlay drawing physics bodies over art is non-negotiable during table work.
5. **Sensors, not solids, for scoring zones**, emitting events onto an EventBus that drives scoring, audio, effects, and the DMD.
6. **The DMD is a self-contained subsystem** (`DotMatrix` surface, `DmdScene` sequences, `DmdQueue` for priority/interrupts) drawing to its own offscreen canvas. Event animations are authored in Claude Design but **baked to sprite-sheet frames at build time** — never live SVG/DOM animation at 60fps in-game.
7. **Physics stays planar** even in the 3D renderer (height is derived at render time). Resist full-3D physics until v1 ships.
8. **Table aspect ratio is sacred** — never stretch the playfield; layout chrome adapts around it (portrait: DMD above table; landscape: side panels).

## Source layout (planned)

`/src` splits into `core` (game loop, physics world, camera, event bus), `entities` (ball, flippers, bumpers, …), `table` (SVG + rules-JSON loading, SVG→fixture parsing), `render` (Renderer2D, later Renderer3D, and `render/dmd`), `audio`, and `game` (scoring, modes, tilt, high scores). See §3 of the plan for the full tree.

## Other constraints

- **IP:** "in the spirit of" the DICE games, not a clone — table themes, art, names, and music must be original.
- **Visual consistency:** the global Claude Design style guide (plan §5a) gates all art work — no individual asset is authored before it exists.
- Browsers require a user gesture before audio starts; kick off the AudioContext on first input.
- Milestone 1 (flipper feel) is make-or-break — expose physics tuning constants in a debug panel rather than hardcoding.
