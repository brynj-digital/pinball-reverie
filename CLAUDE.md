# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

Milestones 0–8 built; M9 (the Three.js renderer) and M10 (multi-table + Tidebreaker, below) are in progress on this branch (`feature/renderer-3d`): `src/render/Renderer3D.ts` implements the same `Renderer` seam — planar physics maps `(x, y)` → scene `(x, z)` per plan §7, the playfield SVG raster textures the table plane and its collision polylines are extruded into rails so 3D walls match physics by construction, and the DMD/backglass mount as a DOM side panel; a "3D camera" settings row switches tilted perspective ↔ top-down orthographic (a WebGL-drawn classic view, persisted in `pinball-3d-view-v1`) via the optional `Renderer.setView3D` seam method. Players toggle 2D ↔ 3D in the settings overlay (persisted in `pinball-render-mode-v1`, deliberately outside `Tuning` so tuning resets can't flip it; render scale is likewise per-renderer outside `Tuning` — `pinball-render-scale-2d-v1` default 1, `-3d-v1` default 0.75, the settings slider edits the active mode's value); `Game.applyRenderMode` swaps renderer + canvas at runtime (a 2D and WebGL context can't share one canvas) and three.js loads lazily as its own chunk, so 2D players never download it. Stack: Vite + TypeScript + Planck.js; Canvas-2D renderer behind the `Renderer` interface; scrolling camera; full element set (flippers, bumpers, slings, drop-target bank, rollovers, orbit + spinner, telescope kickout scoop — `src/entities/Kicker.ts` captures the ball for the DMD "observation" then ejects at `tuning.kickerEject`; the scoop geometry never traps, only the sensor+hold logic keeps a ball) scoring via the EventBus; and the M4/M5 layer — game flow (attract → 3-ball play → game over), ball-saver, nudge/tilt, moon-lane bonus multipliers, localStorage high scores, the DMD subsystem (`src/render/dmd/`: `DotMatrix` 128×32 amber surface + 5×7 bitmap font, `DmdScene`, `DmdQueue`; Game owns it, renderers only composite its canvas), baked DMD event animations (SVG sprite-strip masters in `design/dmd-scenes/` quantized to dot frames at load by `dmd/bake.ts` — never live SVG on the grid), in-world juice (camera shake, pooled impact rings, ball trail), and audio (`src/audio/`: `AudioEngine` — Web Audio buses + synthesized chip SFX per event, unlocked on first gesture; `ChipMusic` — procedural chiptune sequencer playing an original theme; tracker-module `.xm` playback can later replace `ChipMusic` on the same music bus; volumes in the tuning panel). M7: scoring values and mode parameters live in `design/tables/moondial/rules.json` (never hardcode points — `Scoring.ts` reads base awards; `src/game/moondial.ts` runs orbit combos, end-of-ball bonus, the LUNAR ECLIPSE mode and the telescope sightings ladder — escalating scoop awards that wrap, the last spotting an orbit toward the eclipse — from it); backglass art at `design/tables/moondial/backglass.svg` renders in the side panel under the DMD. M8: attract-mode lamp show, DMD high-score initials entry (flippers cycle, plunger confirms), and a player-facing settings overlay (Esc — volumes + full key remapping via `src/ui/SettingsPanel.ts`; `Input` bindings persist in localStorage; game pauses while open). The 2D renderer is **plain Canvas 2D by explicit decision — keep it that way** (no PixiJS/WebGL 2D); it stays the default mode, with `Renderer3D` as the opt-in alternative behind the same interface. When changing any `DEFAULT_TUNING` value, update `docs/tuning/current-defaults.md` (change log + rationale); `docs/tuning/original-defaults.md` is a frozen baseline. The build plan at `docs/plans/pinball-build-plan.md` remains the source of truth — **read it before any implementation work**. Flipper feel is player-approved; physics tuning stays exposed in the debug panel.

**M10 (multi-table):** the engine is table-agnostic behind two seams — `src/table/specs.ts` (`TableSpec`: per-table geometry/scoring-rules/logic, Node-safe, consumed by Game AND the sim scripts) and `src/table/assets.ts` (Vite-only raw SVG/DMD-scene/song imports). Per-table entity constants live in `src/table/defs/<id>.ts` (`src/table/geometry.ts` keeps only shared types + flipper/ball constants); per-table rules logic (modes, multiplier lanes, kicker award ladders, DMD narration) implements `src/game/TableLogic.ts` — `src/game/moondial.ts`, `src/game/tidebreaker.ts` (the old `game/Modes.ts` is gone). Players switch tables in the settings overlay (persisted in `pinball-table-v1`, applied via reload in `main.ts` — the renderer swap stays live, the table swap deliberately doesn't); high scores are per-table. New entities: `Subway` (scripted under-field transit along a height profile) and multiple `Kicker`s per table incl. lit-gated kickbacks (`TableLogic.kickerLit`/`onCapture`); `Game` runs an out-of-bounds net that drains a ball escaping the table envelope; soak flags any OOB ball as a failure.

**M11 (true height — the 2.5D surface model, plan §7a; supersedes M10's collision layers):** the ball carries one real scalar `z`, owned by `src/table/Surfaces.ts` (`HeightState`, Node-safe, shared by Game and the sims; stepped per fixed physics step — `applyForces` before `world.step`, `step` after). Ramps are **surfaces** (height profiles grouped by `data-surface` + `data-surface-width`): the ball attaches where a surface meets its height, feels `−m·g·∇h` in the plane (climbs decelerate, stalls roll back out of the mouth), and flies off drop-off ends ballistically (airborne under real normal gravity, lands on the best surface below). There are NO layer-switch sensors and no `Ball.setLayer` — entry/exit is geometry. Collision is height-banded per contact via `PhysicsWorld`'s `pre-solve` gate (`setZGate`/`contactApplies`): plain walls are ball-height field furniture, `data-z="all"` walls are full height (shell + lane wall), `data-surface` rails follow the LOCAL surface height (low at the mouth, high at the crest). Sensors may carry `data-z-min/max` admission bands, checked by event consumers (`sensorApplies`) plus a resense pass on support change (`sensorsTouching`) for balls that land inside a zone. Authoring rules live in STYLE-GUIDE §4: mouths point down-table with attach zones clear of furniture, **descents end in the air ≥12 mm over a clear landing zone** (a rail descending to ground through live traffic physically blocks it — that's why both tables' habitrail/coaster drop-offs are ballistic now), rails crossing furniture keep ~7 mm clearance over its top, and sensor bands sit ~4 mm loose (begin-contact fires on edge-touch). Table logic keys ramp rides off `surface` bus events (enter/leave with position) — awards fire on leave-at-the-far-end, guarded by boarded-at-the-mouth flags. `snapshot.ball.h` is the true z; `ball.layer` survives as a derived render hint (1 riding/flying, −1 subway). Table 2 is **Tidebreaker** (abyssal salvage dive — design truth in `design/tables/tidebreaker/BRIEF.md`, incl. v1 deltas from the concept): D-I-V-E depth gauge → bonus multiplier + escape-hatch kickback, winch ramp + habitrail circuit (layer 1, spinner as the reel), dive-bell haul ladder, trench + gutter subways, airlock bank recessed in the orbit wall (right-flipper cross shot), LEVIATHAN wizard mode; abyss field-token variant in `design/tokens.css`; per-table chiptune themes in `src/audio/songs.ts` (`ChipMusic` performs a passed `Song`). The drain sensor is ignored while a kicker/subway holds the ball captive — saving subways (Tidebreaker's gutter, Midway's chicken exit) legitimately cross the drain zone; Game, simcheck and soak all carry the same guard.

**Table 3 is Midnight Midway** (funfair after dark — design truth in `design/tables/midway/BRIEF.md`, incl. v1 deltas; the lineup's nearest IP adjacency: the brief's §0 lists what must NOT resemble Pinball Fantasies' Partyland). It introduced the engine's **third flipper**: `TableGeometry.flippers.upper` (optional `{x, y, side}`, placed by `anchor-flipper-upper` and validated like the pair), the `upper` Input action (defaults to sharing the right-flipper keys; remappable in settings), and Game/simcheck/soak drive `flippers[2]` when present. THE MALLET (upper-right, main layer, base sealed 12 mm off the lane wall; the lane wall's top curls into a shield so arch drops shed onto the bat instead of perching) owns the HIGH STRIKER: a layer-1 wire lane over the dodgem arena to a bell above the P lane, graded by `sensor-lane-striker-a/b` timing gates in `src/game/midway.ts` (DING/STRONG/FAIR; roll-back = WEAK) with an up-only entry throat of small zones centred on the lane's centreline (mid-bat swings launch past the root curl; a zone edge within a ball radius of a channel wall switches an embedded ball and the solver ejects it as a layer-1 ghost — soak-found, hence small centred zones). Other rides: COASTER (left-flipper layer-1 circuit, two-crest chained height profiles, opposite-handed from the winch), GHOST TRAIN subway behind a turnstile spinner, DROP TOWER bank in the lane wall (left-flipper cross shot) lighting the PRIZE BOOTH scoop ladder, the ferris-wheel gondola ring (5 lamps; P-A-R-K completions, striker DINGs and the PANDA load gondolas — a full wheel steps the bonus-X), HAND STAMP kickback, CHICKEN EXIT (right-outlane subway back to the plunger lane when lit), and the RIDE PASS (punch all five rides) → FIREWORKS FINALE wizard mode. Carnival field variant + marquee-bulb token in `design/tokens.css`.

**M12 (built) — Table 4 is The Night Mail** (overnight mail train racing the dawn — design truth in `design/tables/nightmail/BRIEF.md` incl. v1 deltas; smoke field variant + signal green in `design/tokens.css`). The feature-engine table — all seven brief-§7 features exist:
- **New entities** (SVG contract in STYLE-GUIDE §4; `npm run entcheck` verifies them in a synthetic world): `src/entities/Diverter.ts` (THE POINTS — one solid blade at a time from `collision-diverter-<id>-<blade>` paths, swapped post-step, polled via `TableLogic.diverterBlade`; every gantry target hit throws them), `Lift.ts` (the incline: `sensor-lift-<id>` capture, z follows `height-profile-<id>`, releases AIRBORNE at hTo via `HeightState.transitTo`/`endTransitAirborne`; supportName "lift", layer +1), `Magnet.ts` (the mail hook: per-step radial pull while `TableLogic.magnetLit`, capture→hold→fling, ground balls only), `Disc.ts` (the turntable: fixtureless tangential velocity-coupling, `TableLogic.discSpin`). Field forces run in the fixed-step `beforeStep`; captures join the captive guards (drain, OOB, ball-saver, soak stuck-window).
- **Multiball** (engine-wide): balls carry `ballId` on their fixture tag; the z-gate and sensor/hit events resolve per ball; Kicker/Subway/Lift/Magnet store their captured Ball (`capture(ball)` / `holds`/`carries`); `Game.extraBalls` + `TableLogicCtx.addBalls` serve staggered extras that drain/OOB silently, the primary promotes so the ball ends only when the LAST drains; camera follows the lowest live ball outside the shooter lane; `WorldSnapshot.extraBalls` renders in both renderers (2D two-pass vs the elevated overlay, 3D pooled mesh clones). The headless sims stay single-ball (no addBalls) by design — modes must remain playable single-ball.
- **DMD video modes**: `Kicker.beginExtendedHold`/`release` behind `TableLogicCtx.holdScoop`; modes are TIMER-driven with the scene as pure display (sims have no DMD — an unclosed hold is a stuck ball). Pattern: SIGNAL BOX in `src/game/nightmail.ts`.
- **Match sequence** (all tables): `MatchScene` (DmdScene) + `Game.makeMatchScene` in both game-over paths; ~10% odds, ceremonial free game.
- **Physical ball locks**: `TableLogicCtx.lockBall(kickerId, berth)` transfers a kicker-held ball to a visible parked berth (gravity off, eased in place, events inert, persists across balls, cleared at game end) and serves a fresh ball if it was the last live one; `releaseLocks()` returns them to play as multiball extras. The Night Mail's siding wagons use it; sims keep the virtual clunk-and-release. `Diverter.setBlade` defers a swap while any live ball overlaps the incoming blade (multiball safety).
Night Mail specifics: `src/game/nightmail.ts` (points/branch, Express pair via ramp-entry/exit, M-A-I-L timetable with lane-change + persistent letters, exchange snag ladder-spotting, sorting mailbag ladder → STRONGBOX lights SIGNAL BOX, gantry→LOCK→siding coupling (physical locks in-game) → DEPARTURE 3-ball, TERMINUS+departure+snag → THE CONNECTION); per-table song `NIGHTMAIL_SONG`; 6 baked DMD scenes (board/exchange/coupling/tunnel/connection/sorting); `scripts/feature-rates.tmp.ts` knows the M12 entities. Playability-solved per the brief's v1 deltas: the M-A-I-L windows (apex cover + two flank mouths — a full crest window kills the orbit), the 34 mm magnet field (50 mm reached through the orbit wall and stalled the express), berths beside the through-lane.

**Table 5 is Small Hours** (rooftop pirate radio until dawn — renamed from "Night Waves" 2026-07-17, id `smallhours`, with localStorage migrations for the saved table + high scores; design truth in `design/tables/smallhours/BRIEF.md`, incl. v1 deltas; §0 lists the distance to keep from Pinball Dreams' Beat Box). The **integration table**: deliberately NO new engine entities — every mechanic re-points existing machinery. THE DIAL (the launch-channel spinner) is a *selector*: spins tune the transmitter, arming the AERIAL RUN's SIGNAL BOOST and drifting the record deck (`Disc`, full spin in modes). The Aerial Run is an M11 climbing surface up the left side (mouth at x 168 above the left flipper — 163 wedged the sling; straddles the sweep channel at h34; back at `data-z-max="10"`), releasing airborne at the mast — the drop feeds the W-A-V-E lanes (kept, the incline's happy accident again). W-A-V-E advances THE CLOCK (1 AM→5 AM bonus-X ladder, lane-change + persistent letters); THE PHONE scoop runs the request ladder → MYSTERY B-SIDE lights REQUEST SHOW (holdScoop video mode, cue SIDE A/B); the FADER BANK (drop targets recessed in the lane wall, left-flipper cross shot — the airlock mirrored) lights CALLER at THE SWITCHBOARD — a **sensor-only capture zone** (no pocket geometry, `data-z-max="8"` so riders overhead never trip it) whose captures park as physical locks on the hold line; three callers = ON AIR 3-ball with the PERFECT SEGUE (both jackpot shots inside `segueS` pays double). DEAD AIR is the table's own hazard (idle `warnS` on the score clock → listeners/bonus units tune out; armed by the first score of each ball, suspended during the video-mode hold). THE SIDE DOOR (right-outlane subway) returns to the **left inlane** — the only outlane in the lineup handing the ball to a live flipper. THE DAWN CHORUS wizard = 5 AM + one ON AIR + one boost, started at the phone. Style guide additions shipped with it: rooftop field variant + **transmitter amber** (`--amber-400/600`, the fifth neon family; amber ramp glass). The aerial pays from 1.8 m/s (speed-sweep verified, no mid-run hop); random-soak ride rate 0/600 s at ~19–32 boards is the proven coaster's class — the boost is an aimed shot by design. Watch item in the brief: lane-set completions run ~1/600 s (all four lanes feed across seeds; open apex-cover flank mouths only if real play shows the clock stalling).

**M13 (built) — Table 6 is The Sump** (the storm-drain junction under the city — design truth in `design/tables/sump/BRIEF.md`, incl. the clearance-solving deltas; culvert field variant + **flood red** `--red-400/600` in `design/tokens.css`). The lower-playfield table: envelope 575×1250 (per-table data; STYLE-GUIDE §3 amended), the **sump chamber** below the lineup's usual floor with a **second full flipper pair on the main left/right buttons** (`TableGeometry.flippers.mini`, `anchor-flipper-mini-*` — Game, sims, DevTable and both renderers handle it). THE FLOODGATE (two-blade diverter) makes a lit centre drain the way IN: shut = one continuous 28° blade→shelf→void slide to the true drain; open = the OFFSET chute drops the ball onto the left mini bat (a centred chute fell straight through the mini tip gap; three roof-and-shed fixes cradled balls at cap corners — all simcheck-found, in the brief). SUMP PLAY runs at ×water level; VALVES → RETURN PIPE subway to the left inlane → outflow ladder + gate relight; S-U-M-P level ladder, pump-house ladder, HIGH WATER wizard (gate locked open), THE READING skill shot.

**M14 (built) — Table 7 is Glasshouse** (the botanic glasshouse at night — design truth in `design/tables/glasshouse/BRIEF.md`; conservatory field variant + **orchid rose** `--rose-400/600`). The widebody (660×1050) with the lineup's first **LEFT-hand plunger**: `table.plungerSide` + the `inShooterLane`/`onPlayfieldSide` helpers in `src/table/geometry.ts` route every lane test (Game, camera, sims, the touch plunger corner) — never hand-roll a `laneWallX` comparison. Double inlanes each side (POLLEN COUNT: 4 return lanes → bonus X + THE MISTER kickback), the free-standing BLOOM 5-bank under a 20° shed roof (the roof took four shapes — see the brief), THE VINE RUN (the only LATERAL surface: two chained profiles, 0→32 mm in the first 83 mm so its mid-field rails gate off ground balls), roving-lamp phototaxis (a moving jackpot), LIGHTS OUT video mode, SWARM multiball, THE CENTURY BLOOM wizard, NIGHT SHIFT skill shot. Build lessons in the brief: the arch is the standard shell mirrored with an identity-slope-at-both-ends warp (a flat widebody crown stalls dying launches — anything under ~17° holds a resting ball) and the orbit is a constant 65 mm inward offset; the right-shell deflector and two crest windows over lanes 2/3 arrived via feature-rates (bumpers were at 0/600 s without them).

**M15 (built) — Table 8 is Summit** (the last cable car to the weather station — design truth in `design/tables/summit/BRIEF.md`; alpine field variant + **aurora ice** `--ice-400/600`). The playable-elevation table: THE TERRACE is a flat h34 surface (the aerial run's proven straddle height) the ball lands on and PLAYS on, with the lineup's first **z-banded flipper** (`flippers.upper.z` — the bat's fixtures gate at platform height via the existing pre-solve; entcheck carries the synthetic proof). The cable car is the M12 Lift unchanged (airborne release 10 mm over the deck + M11 ballistic landing — the planned "grounded release" variant proved unnecessary); instruments are z-banded pads; THE LAUNCH vs gutter dribble is split by the terrace leave event's y. Ground traffic — launches, lanes, the orbit sweep — passes UNDER the platform (suite-asserted). FORECAST bonus ladder, WIND/gale (the car closes), CORNICE avalanche + SNOW JACKPOT, LAST CAR UP multiball (virtual locks per the brief's cut line), THE AURORA wizard, LAST CAR skill shot. Watch item: P-E-A-K completions run 0/600 s in random soak (lanes feed from below only; the crest-window option is pre-scoped in the brief).

**Differentiation pass (2026-07-17; plan + audit in `docs/plans/table-differentiation-plan.md`, per-table deltas in each BRIEF):** the 2026-07-16 layout audit found tables 2–5 sharing a byte-identical bottom third and a fixed 3-bumper/3-target/left-eject/no-skill-shot kit; STYLE-GUIDE §4 now carries the **no-copy-paste rule** and the **`sensor-skill-<name>` soft-plunge skill-shot pattern** (fires `TableLogic.onSkillShot(id, speed)`; ≤ `rules.skill.maxSpeed` ≈ 0.75 m/s qualifies — every table has one, once per ball). Engine seams added: `TableLogic.slingBoost?()` (kick multiplier, applied in Game + all three sims), `TableLogicCtx.saverActive?()` (sims stub an 8 s post-launch window), and the entcheck-proven **inert diverter blade** (retractable-post pattern). Per table: Moondial grew **THE GNOMON** (retractable centre post between the flipper tips — up while the saver is live and during LUNAR ECLIPSE; diverter reuse) + FIRST LIGHT; Tidebreaker left the shared kit (**pressure slings**, bespoke verts + flatter kick `(±0.91,−0.415)`; the airlock is now the 4-target **L-O-C-K** bank) + SOUNDING; Midway swapped the standard nest for **THE DODGEMS** (five r22 bumper cars; the soak-found dead-end strip behind the coaster lift hill is fixed by a **sloped coaster-back**) + TEST YOUR STRENGTH; the Night Mail's sorting office keeps its spot but **ejects RIGHT** (the lineup's only right-hand scoop return — relocation was tried and abandoned, see its BRIEF) + THE SIGNAL; Small Hours got **TUNED slings** (1.15× while TUNED via `slingBoost`), the phone moved to (300,480), + NIGHT OWLS. All verified per the feature-difficulty rule (before/after `feature-rates`, soak seeds, briefs carry the numbers).

**DMD pass (2026-07-17, PR #22):** the display got its presentation layer, engine-wide. `DotMatrix` carries an 8×12 `BIG_DIGITS` font (`bigText`/`centerBigText`) — the score scene draws tall digits with a live per-table ticker under it (`TableLogic.dmdStatus?()`, ≤~21 glyphs); holding both flippers 1.8 s in play pages `TableLogic.statusReport?()` (classic status report); `DmdQueue` wipes every scene change (0.16 s column reveal via `DotMatrix.copyGrid`/`overlayFrom` + scenes' `invalidate()`); ball end plays `BonusScene` (units × multiplier count-up; fireworks before a high-score entry); attract is a show reel (title → tips from `TableSpec.attractTips` → baked strips → top-3 scores). Every timed mode ends with a **MODE TOTAL recap page** (record `ctx.scoring.total` at mode start). **Every table now has a DMD video mode** on the SIGNAL BOX pattern — moondial ALIGN THE SCOPE, tidebreaker SONAR SWEEP, midway RING THE BELL, sump PRESSURE TEST, summit HOLD THE CABLE (rules blocks `alignScope`/`sonar`/`bellGame`/`pressureTest`/`cable`), each lit by its scoop ladder wrapping and **started from `onCapture`, never the raw sensor** (`beginExtendedHold` no-ops before the kicker holds the ball — the pass's one found bug); simcheck carries hold-past-holdS/release-on-timer checks for all five.

**Table 9 is Thunderhead — GREY-BOX PROTOTYPE, gated (2026-07-17):** the storm-watch airship (design truth + build record in `design/tables/thunderhead/BRIEF.md` §9). The brief's §7.1 gate holds: **staggered flippers** (left bat 30 mm high at (178,920), right standard at (342,950), x pulled in so the diagonal tip gap keeps the lineup's ~39 mm) need explicit player feel sign-off before master art, the storm-yellow/stratus style-guide amendment, or the final mix — until then the table ships as a playable bare-strokes grey box (selectable in settings, `tagline: "GREY-BOX PROTOTYPE"`). Structure: **zero bumpers** (the only such table); the chaos engine is two CHARGE CELL magnets with the new **directed-fling seam** (`Magnet.flingDir` ← `TableLogic.magnetFling` polled per frame in Game + both sims, entcheck-proven; flings deliver to the bat that makes the next STORM ROUTE shot — aiming at the shot itself crossed the spine rails). Rules: G-A-L-E → bonus X + charge, cell fling → spine → circuit → nacelle route → STRIKES → SQUALL multiball, BALLAST ×2 + saves, LIGHTNING WATCH video mode, THE EYE wizard, STORM GLASS skill shot; per-ball STATIC kickback (consumed/relit-by-ballast) is the drain-family lever, and crest windows over lanes 2/3 feed G-A-L-E from the circuit (glasshouse's fix). Soak: 0 stuck across seeds, drains 44–53/600 s — in the lineup family, the brief's make-or-break number. The build's four left-wall trap passes (throat pinch, deflector/orbit-cap wedge, merged shed wall with proud targets, shortened spine west rail) and the self-relighting-kickback loop are §9 reading before touching that region.

The global style guide (plan §5a, milestone 0.5) lives at `design/STYLE-GUIDE.md` with tokens in `design/tokens.css` and rendered specimens in `design/previews/` — these are the editable masters, synced to the Claude Design project "Pinball Reverie — game design system". **All art must inherit from it** (palette tokens, 1 SVG unit = 1 mm, collision/sensor/anchor layer naming, ball-gap rules); amend the guide before introducing any new color or convention.

Commands: `npm run dev` (Vite dev server), `npm run build` (typecheck + bundle), `npm run simcheck [table]` (headless physics smoke test, every table by default — run after any physics or playfield-SVG change), `npm run soak [seed] [table]` (randomized 10-min play sim that flags ball traps; runs every table by default), `npm run entcheck` (M12 entity behaviour checks in a synthetic world — run after touching Diverter/Lift/Magnet/Disc). Milestone 3.5 is done: table 1 is **Moondial** (night-observatory theme) and its shape truth is `design/tables/moondial/playfield.svg` — collision walls, sensors, anchors and height profiles are parsed from named layers by `src/table/SvgCollision.ts` (the visible wall strokes ARE the collision paths); each table's entity placements in `src/table/defs/<id>.ts` are cross-checked against its SVG's `anchor-*` markers at load. Run simcheck + soak after ANY edit to a playfield SVG.

## What this is

A browser-based retro pinball game in the spirit of Pinball Fantasies / Pinball Dreams: a vertically-scrolling tall playfield (several screens high), realistic physics, a dot-matrix display (DMD) for score and event animations, and chiptune audio. All visuals are authored in Claude Design as editable SVG masters.

## Planned stack

Vanilla TypeScript + Vite (no framework). Physics: **Planck.js** (Box2D port — chosen over Matter.js for continuous collision detection with fast small bodies). Renderer v1: 2D Canvas/PixiJS; v2: Three.js. Audio: Web Audio API + tracker-module playback (`.xm`/`.mod`) for music. Once scaffolded, expect standard Vite commands (`npm run dev`, `npm run build`).

## Architecture invariants

These are the decisions the whole plan hinges on — don't compromise them for convenience:

1. **Physics ↔ renderer decoupling.** Game state lives in the Planck world; renderers only *read* it each frame through a `Renderer` interface (`init`, `drawFrame`, `spawnEffect`). This is what makes the later 2D→3D swap possible. `Game` must never know which renderer it's talking to.
2. **Simulate in meters, not pixels.** Box2D wants 0.1–10m bodies (ball ≈ 0.027m). Scale to pixels only at render time.
3. **Fixed timestep + bullet ball.** Step physics at a fixed 1/120s; mark the ball as a bullet body for CCD. Variable timesteps and tunnelling are the two classic pinball failure modes.
4. **SVG is the single source of truth for table shape.** Collision geometry is derived from named layers/paths in the Claude Design playfield SVG (`collision-wall-*`, `sensor-ramp-entry-*`); the table's JSON carries only rules metadata (scoring, modes, lights). Never hand-author collision bodies that duplicate drawn geometry. A debug overlay drawing physics bodies over art is non-negotiable during table work.
5. **Sensors, not solids, for scoring zones**, emitting events onto an EventBus that drives scoring, audio, effects, and the DMD.
6. **The DMD is a self-contained subsystem** (`DotMatrix` surface, `DmdScene` sequences, `DmdQueue` for priority/interrupts) drawing to its own offscreen canvas. Event animations are authored in Claude Design but **baked to sprite-sheet frames at build time** — never live SVG/DOM animation at 60fps in-game.
7. **Physics stays planar** even in the 3D renderer (height is derived at render time). Resist full-3D physics until v1 ships.
8. **Table aspect ratio is sacred** — never stretch the playfield; layout chrome adapts around it (portrait: DMD above table; landscape: side panels).

## Source layout (planned)

`/src` splits into `core` (game loop, physics world, camera, event bus), `entities` (ball, flippers, bumpers, …), `table` (SVG + rules-JSON loading, SVG→fixture parsing), `render` (Renderer2D, later Renderer3D, and `render/dmd`), `audio`, and `game` (scoring, modes, tilt, high scores). See §3 of the plan for the full tree.

## Other constraints

- **IP:** "in the spirit of" the DICE games, not a clone — table themes, art, names, and music must be original.
- **Visual consistency:** the global Claude Design style guide (plan §5a) gates all art work — no individual asset is authored before it exists.
- Browsers require a user gesture before audio starts; kick off the AudioContext on first input.
- Milestone 1 (flipper feel) is make-or-break — expose physics tuning constants in a debug panel rather than hardcoding.
